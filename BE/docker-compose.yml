version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: be_server
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
